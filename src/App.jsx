import Artifact from './Artifact'

function App() {
  return (
    // We remove all the classes here so the Artifact can control the full screen
    <div>
       <Artifact />
    </div>
  )
}

export default App